"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var megalodon_1 = __importDefault(require("megalodon"));
var BASE_URL = 'https://mastodon.social';
var access_token = process.env.MASTODON_ACCESS_TOKEN;
var client = megalodon_1.default('mastodon', BASE_URL, access_token);
var stream = client.publicStream();
stream.on('connect', function (_) {
    console.log('connect');
});
stream.on('not-event-stream', function (mes) {
    console.log(mes);
});
stream.on('update', function (status) {
    console.log(status);
});
stream.on('notification', function (notification) {
    console.log(notification);
});
stream.on('delete', function (id) {
    console.log(id);
});
stream.on('error', function (err) {
    console.error(err);
});
stream.on('heartbeat', function () {
    console.log('thump.');
});
stream.on('connection-limit-exceeded', function (err) {
    console.error(err);
});
setTimeout(function () {
    stream.stop();
}, 10000);
