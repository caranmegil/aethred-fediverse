"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var megalodon_1 = __importDefault(require("megalodon"));
var log4js_1 = __importDefault(require("log4js"));
var BASE_URL = 'wss://pleroma.io';
var access_token = process.env.PLEROMA_ACCESS_TOKEN;
var client = megalodon_1.default('pleroma', BASE_URL, access_token);
var stream = client.userSocket();
var logger = log4js_1.default.getLogger();
logger.level = 'debug';
stream.on('connect', function () {
    logger.debug('connect');
});
stream.on('pong', function () {
    logger.debug('pong');
});
stream.on('update', function (status) {
    logger.debug(status.url);
});
stream.on('notification', function (notification) {
    logger.debug(notification);
});
stream.on('delete', function (id) {
    logger.debug(id);
});
stream.on('error', function (err) {
    console.error(err);
});
stream.on('heartbeat', function () {
    logger.debug('thump.');
});
stream.on('close', function () {
    logger.debug('close');
});
stream.on('parser-error', function (err) {
    logger.error(err);
});
